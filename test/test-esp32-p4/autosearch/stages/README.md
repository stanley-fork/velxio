# Stages — roadmap ESP32-P4 en QEMU

Tablero de estado: **una fila por fase**, ordenadas. Lo que viene primero es lo que arranco al volver. El detalle por fase está en su archivo individual.

| # | Fase | Estado | Commit | Archivo |
|---|---|---|---|---|
| 0 | Machine scaffold + memmap (TRM Cap 7) | ✅ done | `d7969f4` | [phase_0_scaffold.md](phase_0_scaffold.md) |
| 1.A | UART0 real + `-bios` loader | ✅ done | `24e67a8` | [phase_1a_uart.md](phase_1a_uart.md) |
| 1.B | 22 named peripheral stubs | ✅ done | `c976734` | [phase_1b_stubs.md](phase_1b_stubs.md) |
| 1.C | eFuse + SYSTIMER + GPIO reales | ✅ done | `b9abf37` | [phase_1c_efuse_systimer_gpio.md](phase_1c_efuse_systimer_gpio.md) |
| 1.D | `-kernel` ELF loader + extflash + trampolín | ✅ done | `cd03e7a` | [phase_1d_elf_loader.md](phase_1d_elf_loader.md) |
| 1.E | SP init + CSRs custom + ROM ret-fill + PF_X overlay | ✅ done | `b0c4aad` | [phase_1e_unblocks.md](phase_1e_unblocks.md) |
| 1.E.bis | `-bios` acepta ELF + ROM oficial Espressif + trampolín relocalizado | ✅ done | `e05e201` | [phase_1e_bis_rom_loader.md](phase_1e_bis_rom_loader.md) |
| 1.F-lite | RVA+RVF + CLIC MMIO + flash bypass + smart sysreg stubs | ✅ done | `fe94cea` | [phase_1f_lite_unblocks.md](phase_1f_lite_unblocks.md) |
| 1.F.bis | More smart-stub overrides + runtime patches → spi_flash_cache_enabled | ✅ done | `d4505f8` | [phase_1f_bis_more_unblocks.md](phase_1f_bis_more_unblocks.md) |
| 1.F.ter | `__assert_func` no-op patch → past cache asserts, into regi2c spin | ✅ done | `19537aa` | [phase_1l_iterative_patches.md](phase_1l_iterative_patches.md) |
| **2** | **PROFESSIONAL ROADMAP** — implementación completa sin hacks | 🚧 wip | — | [PHASE_2_PROFESSIONAL_ROADMAP.md](PHASE_2_PROFESSIONAL_ROADMAP.md) |
| 2.A.1 | Flash blob via `-drive if=mtd` + ROM ret-fill sync write | ✅ done | `07ac21b` | (see roadmap) |
| 2.A.2 | Cache/MSPI smart stubs + ROM patch → **UART output real** | ✅ done | `4253b3e` | (see roadmap) |
| 2.A.3 | Section-data pass + reset-cause/strap overrides → **full ROM banner** | ✅ done | `780ad0c` | [phase_2a3_section_data_banner.md](phase_2a3_section_data_banner.md) |
| 2.A.4 | `_init` skip-unpack-clear patch → **ROM advances to CLIC** | ✅ done | `db700e4` | [phase_2a4_runtime_overwrite.md](phase_2a4_runtime_overwrite.md) |
| 2.D | CLIC backing-RAM + Cache freeze mirrors → **ROM reaches bootloader** | ✅ done | `3a1e6ed` | [phase_2d_clic_cache.md](phase_2d_clic_cache.md) |
| 2.A.5 | `ets_loader_map_range` patch + flash blob reload → **ROM jumps to bootloader entry** | ✅ done | (this commit) | [phase_2a5_bootloader_load.md](phase_2a5_bootloader_load.md) |
| 2.B.regi2c | LPPERI clock-enable + regi2c done bit → **bootloader runs 6.4s of init** | ✅ done | (this commit) | [phase_2b_regi2c.md](phase_2b_regi2c.md) |
| 2.B.boot_comm | Cache MMU block 63 emulator → **chip ID check passes!** | ✅ done | `64e2cd2` | [phase_2b_boot_comm.md](phase_2b_boot_comm.md) |
| 2.B.post_qio (perf) | MMU refactor to eager-copy → **5x faster boot progress** | ✅ done | `947fba8` | [phase_2b_post_qio.md](phase_2b_post_qio.md) |
| 2.I.sha (stub) | Crypto block stubs + SHA_BUSY=0 → ROM `ets_sha_*` returns | ✅ done | `5706e8c` | [phase_2i_sha.md](phase_2i_sha.md) |
| 2.J | `-kernel` ELF path + magic check bypass → **app reaches pmu_hp_system_init** | ✅ done | `887d5d1` | [phase_2j_kernel_elf.md](phase_2j_kernel_elf.md) |
| 2.J.next | CPU1 wait loop bypass → **app runs through call_start_cpu0 + ~30 init fns** | ✅ done | `1f06095` | [phase_2j_kernel_elf.md](phase_2j_kernel_elf.md) |
| 2.J.uart | Investigación: app NOT stuck, ejecutando lento. Requiere scheduler/interrupts. | ✅ analyzed | `adeb86f` | [phase_2j_kernel_elf.md](phase_2j_kernel_elf.md) |
| 2.K.systimer | SYSTIMER OP_REG snapshot protocol — exits IDF poll loops | ✅ done | `e408a19` | [phase_2k_systimer.md](phase_2k_systimer.md) |
| 2.K.init_skip | Skip do_system_init_fn → app reaches **C++ static init + pthread + FreeRTOS port** | ✅ done | `53f8358` | [phase_2k_init_skip.md](phase_2k_init_skip.md) |
| 2.K.frame_info | Skip `__register_frame_info` libgcc DWARF init → reach `vTaskStartScheduler` | ✅ done | `ce6110f` | [phase_2k_init_skip.md](phase_2k_init_skip.md) |
| 2.L | Crosscore err bypass + static idle-task buffers → **xTaskCreate + heap_caps + intr_alloc** | ✅ done | `0cf82e7` | [phase_2l_freertos.md](phase_2l_freertos.md) |
| 2.L.next | Bump allocator + vSystimerSetup bypass → **vTaskStartScheduler runs end-to-end** | ✅ done | `08e104d` | [phase_2l_next_bumpalloc.md](phase_2l_next_bumpalloc.md) |
| 2.M | Bypass scheduler → **main_task + app_main + initArduino executing** | ✅ done | `e555e8c` | [phase_2m_bypass_scheduler.md](phase_2m_bypass_scheduler.md) |
| 2.N | Inline UART writer in app_main → **🎉 "Hello from QEMU ESP32-P4!" 🎉** | ✅ done | `a216796` | [phase_2n_hello_world.md](phase_2n_hello_world.md) |
| 2.O | SYSTIMER 100 Hz tick → CPU IRQ_M_EXT (foundation for FreeRTOS) | ✅ done | `94f989a` `bf7cb47` | [phase_2o_clic_irq.md](phase_2o_clic_irq.md) |
| 2.P | **Investigated**: dropped Phase 2.N → app stuck same as before. Trap to mtvec not firing despite IRQ wiring + CSR enables. Hello-world demo re-enabled. | 🔬 done w/ findings | (this commit) | [phase_2p_real_arduino_attempt.md](phase_2p_real_arduino_attempt.md) |
| 2.Q | **Instrumented esp_cpu IRQ dispatch → root cause: `mtvec=0` when first trap fires (bypass skipped IDF mtvec setup); first trap clears mstatus.MIE permanently.** | ✅ done w/ findings | (this commit) | [phase_2q_irq_diagnostics.md](phase_2q_irq_diagnostics.md) |
| 2.R | **mret stub at `0x4FC1FFB0` + trampoline writes mtvec → end-to-end IRQ delivery validated.** mstatus stable at 0x1888 (MIE=1) across continuous SYSTIMER ticks. | ✅ done | (this commit) | [phase_2r_mtvec_stub.md](phase_2r_mtvec_stub.md) |
| 2.S | **CLIC mode dispatch in `target/riscv/`**: override `write_mtvec` to accept mode 3, dispatch via `*(mtvt + cause*4)`, route SYSTIMER to free cause 17. IDF `_interrupt_handler` now runs on every tick. | ✅ done | (this commit) | [phase_2s_clic_mode.md](phase_2s_clic_mode.md) |
| 2.T | **Investigated**: bypass-dropped flow stuck in `esp_ota_get_running_partition` ESP_LOGE retry loop because `esp_partition_find` returns NULL (partition list empty). NOT a Cache MMU issue (eager-copy works fine). HP_SYS_CLKRST clock-update override verified working. | 🔬 done w/ findings | (prior commit) | [phase_2t_partition_blocker.md](phase_2t_partition_blocker.md) |
| 2.T-fix | **Function-entry stub** at `0x4000549C` returns pointer to fake `esp_partition_t` at `0x4FFA0030`. Bypass-dropped flow now reaches IDF panic handler full register dump (huge progress). New blocker: instruction fetch fault to `0x3FFFF820` (likely fake-struct field deref). | ✅ done | (prior commit) | [phase_2t_fix_partition_stub.md](phase_2t_fix_partition_stub.md) |
| 2.T-fix.next | **Phase 2.M typo fix**: `0xFA4FC06F` → `0xFA5FC06F` (1-bit `imm[11]` error in JAL encoding). Was jumping to `0x3FFFF820` instead of intended `0x40000020 (setup())`. Now bypass-dropped flow reaches setup(), panic handler completes full output incl. Reboot sequence. | ✅ done | (prior commit) | [phase_2t_fix_next_typo.md](phase_2t_fix_next_typo.md) |
| 2.T-fix.next.next | **Print::write virtual dispatch neutralised** at `0x4000079A` (NULL vtable). New blocker: `uxListRemove` called with garbage pointer (FreeRTOS state corruption from skipped scheduler init). Each subsequent fault is deeper in FreeRTOS — single-patch chain not viable. | ✅ done w/ findings | (prior commit) | [phase_2t_fix_next_next_print_write.md](phase_2t_fix_next_next_print_write.md) |
| 2.U | **🎉 LED BLINK MILESTONE** — hand-rolled asm at end of hello-world bypass toggles GPIO pin 5 at ~80 Hz. Dual-peripheral demo (UART + GPIO concurrently). GPIO model patched to use `fprintf(stderr)` for default visibility. | ✅ done | (prior commit) | [phase_2u_led_blink.md](phase_2u_led_blink.md) |
| 2.V | **3-pin running light @ ~3.5 Hz** cycling pins 5→6→7 via trampoline-to-blob in cache-window IRAM. Discovered: L2MEM is NOT executable under IDF PMP rules; cache window 0x40000000+ is the safe executable region. | ✅ done | (prior commit) | [phase_2v_running_light.md](phase_2v_running_light.md) |
| 2.W | **GPIO_IN register + external input pads** (`qdev_init_gpio_in_named` "esp32p4.gpio.input") + built-in fake button toggling pin 0 every 3 s host wall-clock. Concurrent guest-output + external-input demo working. | ✅ done | (prior commit) | [phase_2w_gpio_input.md](phase_2w_gpio_input.md) |
| 2.W.next | **GPIO_ENABLE pad multiplexer** — `effective = (gpio_out & enable) \| (external_input & ~enable)`. Demo blob now enables pins 5/6/7 first (mimics `pinMode(OUTPUT)`). Pin behaviour matches real silicon. | ✅ done | (prior commit) | [phase_2w_next_enable_gating.md](phase_2w_next_enable_gating.md) |
| 2.X | **GPIO event stream** to JSON-Lines file (env-gated `VELXIO_GPIO_LOG`). Each pin transition appends `{"t_ns":...,"pin":N,"level":M}`. Wall-clock timestamps. Frontend tail-f-able. Default build unchanged. | ✅ done | (prior commit) | [phase_2x_event_stream.md](phase_2x_event_stream.md) |
| 2.X.input | **Reverse channel**: `VELXIO_GPIO_INPUT=/path/to/fifo`. Frontend writes `{"pin":N,"level":M}` lines; emulator parses + forwards to `external_input` pads. Verified: 4 frontend-injected events appear in output log with 500 ms wall-clock spacing. **Bidirectional emulator ↔ frontend channel complete**. | ✅ done | (prior commit) | [phase_2x_input_reverse_channel.md](phase_2x_input_reverse_channel.md) |
| 2.Y | **SYSTIMER-based deterministic delays** — running-light blob now polls SYSTIMER UNIT0_VAL_LO via JAL ra `.delay` subroutine. Each delay = 1.6M ticks @ 16 MHz = exactly 100 ms regardless of host CPU speed. JSON log shows ~0.3% drift (vs >8x with busy-wait). | ✅ done | (prior commit) | [phase_2y_systimer_delays.md](phase_2y_systimer_delays.md) |
| 2.Z | **GPIO pin-transition IRQ wired to CPU**: per-pin `int_ena_mask` (regs 0x70/0x74/0x78) gates `pin_irq[N]`; `gpio.pin[0]` connected to `espressif-cpu-irq-lines[18]`. Demo blob enables pin 0 IRQ. End-to-end verified: fake-button transitions → CPU `line=18 level=1/0`. | ✅ done | (prior commit) | [phase_2z_pin_interrupts.md](phase_2z_pin_interrupts.md) |
| 2.AA | **INT_TYPE filter + multi-pin wiring**: rising/falling-edge masks (regs 0x80-0x98), 8 pins (0-7) wired to CPU lines 18-25. Demo configures pin 0 RISING-only — fake button only fires CPU IRQ on press, not release (pulse semantics). | ✅ done | (prior commit) | [phase_2aa_int_type_filter.md](phase_2aa_int_type_filter.md) |
| 2.AB | **Real-silicon refactor: latched INT_STATUS + shared CPU IRQ**. INT_STATUS reg at 0xA0 latches pending bits per pin; INT_STATUS_W1TC at 0xA4 clears (ISR pattern). Single `intr_out` aggregates to CPU cause 18 — replaces the 8-pin direct wiring. ALL 32 pins now route through this single shared IRQ matching real silicon. | ✅ done | (prior commit) | [phase_2ab_int_status.md](phase_2ab_int_status.md) |
| 2.AC | **LEDC PWM peripheral** at `0x500D3000`. 8 channels of duty-tracking, JSON event emission per duty write, demo blob fades through 0..1792 sawtooth. First NEW peripheral added since the GPIO chain. Unified event stream now carries GPIO + LEDC. | ✅ done | (prior commit) | [phase_2ac_ledc_pwm.md](phase_2ac_ledc_pwm.md) |
| 2.AD | **ADC peripheral** at `0x500DE000` returns 4-second sawtooth via `QEMU_CLOCK_REALTIME`. Demo blob reads ADC each loop and forwards to LEDC duty — physically realistic "analog input drives LED brightness" pipeline. JSON stream now carries 4 event types: start, pin, ledc, adc. | ✅ done | (prior commit) | [phase_2ad_adc.md](phase_2ad_adc.md) |
| 2.AE | **LEDC multi-channel crossfade**: demo blob extended to also drive CH1 = `(0x7FF - sample)` for inverse fade. Pairs of CH0+CH1 events with sums always = 0x7FF prove multi-channel decode works. | ✅ done | (prior commit) | [phase_2ae_ledc_multichannel.md](phase_2ae_ledc_multichannel.md) |
| 2.AF | **LEDC 3-channel RGB rainbow**: demo blob now drives CH0/CH1/CH2 simultaneously. CH2 = `(sample + 0x555) & 0x7FF` — 240° phase-shifted. Validates LEDC decode for channels beyond 1; gives frontend a 3-LED chase pattern. | ✅ done | (prior commit) | [phase_2af_ledc_rgb_rainbow.md](phase_2af_ledc_rgb_rainbow.md) |
| 2.AG | **TIMG (Timer Group) peripheral** at `0x500BC000`. 54-bit counter @ 1 MHz tick anchored to QEMU_CLOCK_REALTIME. Alarm comparator + 50ms QEMUTimer + JSON `event:"timg"`. Self-test pre-program produces 1-Hz autoreload events out-of-the-box. Foundation for Arduino `hw_timer_t` / `attachInterrupt`. | ✅ done | (prior commit) | [phase_2ag_timg.md](phase_2ag_timg.md) |
| 2.AH | **TIMG → CPU IRQ wiring (cause 19)**. INT_RAW & INT_ENA propagates via `qdev_connect_gpio_out_named` to `espressif-cpu-irq-lines`. Live test confirms IRQ line transitions 0→1 at first alarm match (`event:"timg_irq"`). Self-test enables INT_ENA; CPU has MIE=0 in bypass flow so no trap regression. | ✅ done | (prior commit) | [phase_2ah_timg_irq.md](phase_2ah_timg_irq.md) |
| 2.AI | **TIMG DIVIDER respect** — counter time base now scales with bits 21:9 of T0_CONFIG (was hardcoded 1 MHz). `tick_rate = APB(80MHz) / DIVIDER`. Self-test now uses DIVIDER=8000 → 10 kHz tick, counter values 100× smaller while wall-clock cadence preserved. | ✅ done | (prior commit) | [phase_2ai_timg_divider.md](phase_2ai_timg_divider.md) |
| 2.AJ | **ISR + mtvec + MIE install** in demo blob (5 init + 8 ISR instructions). Self-test → one-shot for safety. Pin 8 added to ENABLE mask. **CPU does NOT trap to ISR** on TIMG IRQ — root cause was missing `mie.MEIE`, fixed in Phase 2.AK. | 🔬 done w/ findings | (prior commit) | [phase_2aj_isr_demo_attempt.md](phase_2aj_isr_demo_attempt.md) |
| 2.AK | **🎉 FULL `attachInterrupt()` CHAIN ALIVE** — root cause was `mie.MEIE` (bit 11) not set; mstatus.MIE alone insufficient since IRQ uses MIP_MEIP. Added 3 init instructions (`addi/slli/csrs mie`) and 4 ISR-prefix instructions (`csrr mcause` + `bne` filter). Pin 8 toggles at exactly 1 Hz driven by TIMG hardware-timer interrupt. **First end-to-end CPU trap delivery in emulator history**. | ✅ done | (prior commit) | [phase_2ak_isr_clic_fix.md](phase_2ak_isr_clic_fix.md) |
| 2.AL | **Multi-source ISR (TIMG + GPIO)** — single ISR dispatches on `mcause` to handle both TIMG (cause 19, toggles pin 8) and GPIO (cause 18, W1TC INT_STATUS + toggles pin 9). 21-instruction ISR with branch-chain dispatch. Real-silicon shared-trap-vector pattern. | ✅ done | (prior commit) | [phase_2al_multi_source_isr.md](phase_2al_multi_source_isr.md) |
| 2.AM | **I2C0 master skeleton** at `0x500D2000` — FIFO_DATA + CMD0..CMD7 register decoders emit JSON `event:"i2c"` (fifo_tx bytes + cmd opcodes). Machine self-test fires "BMP280 chip-id read" sequence at boot (8 events). Foundation for sensor demos. | ✅ done | (prior commit) | [phase_2am_i2c_skeleton.md](phase_2am_i2c_skeleton.md) |
| 2.AM.slave | **Synthetic BMP280 I2C responder** — register table with chip_id=0x58 at 0xD0 + time-varying pressure/temperature raw bytes at 0xF7-0xFC. tx_history[2] rolling buffer infers register from second-most-recent FIFO_DATA write. Auto-increment for burst reads. Self-test now reads chip-id back: `i2c_rx reg=208 byte=88`. **Arduino BMP280 chip-id check passes**. | ✅ done | (prior commit) | [phase_2am_slave.md](phase_2am_slave.md) |
| 2.AN | **TIMG1 — second timer group** at `0x500C0000`. Symmetric to TIMG0 with different alarm period (500 ms vs 1 s). JSON events distinguished by `grp:0`/`grp:1`. Stderr tags: `[esp32p4.timg0]`/`[esp32p4.timg1]`. CPU IRQ NOT wired (cause 20 has no ISR handler yet — Phase 2.AN.irq later). Validates device-class reusability via two independent instances. | ✅ done | (prior commit) | [phase_2an_timg1.md](phase_2an_timg1.md) |
| 2.AN.irq | **3-way ISR dispatch (TIMG0 + GPIO + TIMG1)** — TIMG1.intr → CLIC cause 20, ISR extended with third branch (cause 20 → toggle pin 10). 31-instruction ISR. Pin 8 @ 1 Hz (TIMG0), pin 9 on each rising edge (GPIO), pin 10 @ 2 Hz (TIMG1). FreeRTOS-style multi-source IRQ pattern fully working. | ✅ done | (prior commit) | [phase_2an_irq.md](phase_2an_irq.md) |
| 2.AO | **SPI2 master skeleton** at `0x500CC000` — USR-trigger detection on CMD writes, JSON `event:"spi"` with bit-length + W0 buffer contents. Self-test fires 3 events at boot showing ILI9341 init pattern (SWRESET, Power Control A, 5-byte data). Foundation for SPI display/SD/W5500 demos. | ✅ done | (prior commit) | [phase_2ao_spi_skeleton.md](phase_2ao_spi_skeleton.md) |
| 2.AP | **TIMG Watchdog (WDT) stub** — register-level WDT modelling per TRM Ch 17 + IDF `timer_group_reg.h`. WDTCONFIG0/1-5/FEED/WPROTECT at offsets 0x48-0x64. Magic key `0x50D83AA1` unlock per TRM 17.2.2.3 enforced; CONFIG writes silently dropped when locked. Self-test fires 4 events at boot showing canonical "Arduino disableCore0WDT" sequence (unlock + disable + feed + lock). | ✅ done | (prior commit) | [phase_2ap_wdt.md](phase_2ap_wdt.md) |
| 2.AQ | **TIMG1 WDT self-test mirror** — single-line addition: machine init now calls `esp32p4_timg_wdt_self_test(&ms->timg1)` after the TIMG0 one. JSON stream shows 8 wdt events (4 per group) instead of 4. Both groups demonstrate Arduino's `disableCoreNWDT()` pattern. | ✅ done | (prior commit) | [phase_2aq_timg1_wdt.md](phase_2aq_timg1_wdt.md) |
| 2.AR | **HW Random Number Generator (RNG)** at `0x500FC400` — TRM Ch 33. Reads return random uint32 from QEMU host PRNG. JSON `event:"rng"` per access (50ms throttled, with running count). Foundation for `esp_random()` / `random()` / TLS / secure boot. Self-test reads 3 values at boot. | ✅ done | (prior commit) | [phase_2ar_rng.md](phase_2ar_rng.md) |
| 2.AS | **Peripheral relocation to real-silicon addresses**. TIMG0 0x500BC000→0x500C2000, TIMG1 0x500C0000→0x500C3000, I2C0 0x500D2000→0x500C4000, SPI2 0x500CC000→0x500D0000 (per IDF reg_base.h). Bumped TIMG overlay priority 1→3 to override smart_stubs. Added rtc_clk_cal_done bit override at TIMG offset 0x80 (was provided by smart_stub). Updated demo blob ISR `lui` for new TIMG bases. All Phase 2.AR functionality preserved at silicon-correct addresses. | ✅ done | (prior commit) | [phase_2as_relocate.md](phase_2as_relocate.md) |
| 2.AT | **RTC WDT + Super WDT** (LP_WDT block at `0x50116000`) — TRM 17.2 + 17.3. Single device modelling both watchdogs that share the MMIO range. RTC WDT uses key `0x50D83AA1` (same as TIMG); Super WDT uses different key `0x8F1D312A` (paranoid lock for failsafe). Self-test fires 7 events: 4 rtc_wdt (unlock/disable/feed/lock) + 3 super_wdt (unlock/disable/lock — no feed since SWD auto-feeds from HW). **Watchdog inventory now COMPLETE** (4/4 WDTs). | ✅ done | (prior commit) | [phase_2at_lp_wdt.md](phase_2at_lp_wdt.md) |
| 2.AU | **Synthetic ILI9341 SPI responder** — `last_cmd` tracks the first TX byte of each USR transaction. Returns realistic register values for 8 ILI9341 read commands (RDDID 0x04→0x9341, RDDST, RDDPM, etc.). Self-test exercises the read path via direct write+USR fire showing `spi_rx cmd:4 response:37697 (0x9341)`. Arduino TFT_eSPI `Adafruit_ILI9341::begin()` chip-id check would now pass. | ✅ done | (prior commit) | [phase_2au_spi_slave.md](phase_2au_spi_slave.md) |
| 2.AV | **GPIO LEVEL_HIGH/LOW INT_TYPE filters** — completes the 6-value INT_TYPE matrix per TRM Ch 9.5. Adds 2 new aggregate masks at 0xA8/0xB4 (with W1TS/W1TC variants) mirroring Phase 2.AA's rising/falling pattern. Update logic in `esp32p4_gpio_update()` fires + latches `int_status` while level matches. No demo blob change (would deadlock on fake button with no deassertion). **All 6 INT_TYPE values supported**. | ✅ done | (prior commit) | [phase_2av_gpio_level.md](phase_2av_gpio_level.md) |
| 2.AW | **UART TX JSON event tracking** — overrides ESP32P4 UART class `uart_write` virtual to emit `event:"uart_tx"` for every TX FIFO write. 26 events at boot decode as ASCII spelling "Hello from QEMU ESP32-P4!". Frontend can now render `Serial.print()` output as a serial-monitor view. Pattern: chain to parent's saved write pointer + emit JSON. No throttling — every byte matters. | ✅ done | (prior commit) | [phase_2aw_uart_tx.md](phase_2aw_uart_tx.md) |
| 2.AX | **UART RX JSON event tracking** — mirrors 2.AW for the read direction. Overrides `uart_read` virtual, snapshots `fifo8_is_empty(rx_fifo)` BEFORE chaining to parent (silicon-correct: real chip returns 0 on empty read, so post-drain check would lose the distinction). Emits `event:"uart_rx"` only when guest actually consumes a byte — zero-flood on polling loops. Regression-clean (484 events, identical to 2.AW). JSON tracer coverage now COMPLETE (16 event types). | ✅ done | (prior commit) | [phase_2ax_uart_rx.md](phase_2ax_uart_rx.md) |
| 2.AY | **🎉 RMT (Remote Control) — WS2812 NeoPixel** at `0x500A4000`. First brand-new peripheral since the WDT chain. Decodes RMT item streams (32-bit `{lvl1,dur1,lvl0,dur0}` pulses) as WS2812B bits via `dur0 > dur1` heuristic — timing-invariant, no clock-rate plumbing needed. 24 bits packed in GRB order per pixel, JSON event `{"event":"rmt","ch":N,"pixel":P,"r":R,"g":G,"b":B}` per pixel. Self-test at boot fires 3 pixels (pure red/green/blue) validating the full path. Foundation for Adafruit_NeoPixel / FastLED Arduino demos. **17 JSON event types now**. | ✅ done | (prior commit) | [phase_2ay_rmt.md](phase_2ay_rmt.md) |
| 2.AZ | **Multi-UART (UART1..UART4)** at their real-silicon bases (0x500CB000..0x500CE000). Each is a distinct `ESP32P4UARTState` instance sharing the same QOM class — validates the "single static parent_write/read pointer covers all instances" claim from 2.AW. No chardev (real silicon routes via IO MUX); per-instance `port_num` (1..4) disambiguates in JSON. Self-test on UART1 writes "U1" via `address_space_write` → emits 2 `{"event":"uart_tx","port":1,...}` events at boot, proving the multi-instance JSON tagging works. Fills Arduino Serial1/Serial2 (GPS, BT, RS-485) gap. | ✅ done | (prior commit) | [phase_2az_multi_uart.md](phase_2az_multi_uart.md) |
| 2.BA | **🎉 TWAI (CAN bus) skeleton** at `0x500A6000` — automotive Arduino demos. SJA1000-derived register layout per TRM Ch 30.4. CMD.TX_REQ writes decode the TX buffer (TX_FRAME_INFO + ID + DATA) into a structured JSON event with ID, DLC, ext/RTR flags, and data byte array. Standard 11-bit ID encoding `(b0<<3) \| (b1>>5)` math verified via boot self-test (frame ID=0x123 DLC=2 data=[0xDE,0xAD] round-trips perfectly). Extended 29-bit IDs supported by construction. 50 ms throttle prevents JSON flood on 1 Mbit/s CAN bus loops. **18 JSON event types now**. | ✅ done | (prior commit) | [phase_2ba_twai.md](phase_2ba_twai.md) |
| 2.BB | **I2C1 + LP_UART instantiation** — completes the dual-instance bus controller inventory. I2C1 at `0x500C5000` (Arduino Wire1), LP_UART at `0x50121000` (port_num=5). Both reuse existing device classes — pure machine-init boilerplate, no class changes. I2C1 self-test produces a full 9-event BMP280 chip-id read transaction with port=1 (incl. i2c_rx byte=0x58 confirming the responder fired); LP_UART self-test writes "L0" → 2 uart_tx events with port=5. **6 of 6 UARTs and 2 of 2 I2Cs now reachable**. Pattern from 2.AZ proven to scale across multiple device classes. | ✅ done | (prior commit) | [phase_2bb_i2c1_lp_uart.md](phase_2bb_i2c1_lp_uart.md) |
| 2.BC | **Synthetic TWAI RX responder — bidirectional CAN bus**. Mirrors BMP280 I2C / ILI9341 SPI responder pattern: pre-loads canned frame (ID=0x456, DLC=3, data=[0xCA,0xFE,0x42]) into RX buffer, sets STATUS.RX_BUFFER bit; CMD.RELEASE_RX writes consume frame + emit `{"event":"twai_rx",...}` JSON event AND auto-load next rotating-ID frame so successive polls see fresh data. Boot self-test exercises both TX (Phase 2.BA) and RX (new) — round-trip ID encode/decode verified for both directions. Closes the CAN loop: Arduino `twai_receive()` sketches now work. **19 JSON event types**. | ✅ done | (prior commit) | [phase_2bc_twai_rx.md](phase_2bc_twai_rx.md) |
| 2.BD | **MPU-6050 IMU + I2C multi-sensor dispatcher**. I2C responder now branches by 7-bit slave address (`tx_history[0] >> 1`): 0x76/0x77 → BMP280 (existing), 0x68/0x69 → MPU-6050 (new). MPU returns WHO_AM_I=0x68 at reg 0x75, time-varying accel X/Y (sinusoidal ±0.5g), accel Z (~+1g with jitter), gyro X/Y/Z (small drift), internal temp (~25°C). Self-test reads MPU WHO_AM_I → 3rd `i2c_rx` event at boot validates dispatcher. Unlocks Arduino IMU demos (Adafruit_MPU6050, balance robots, drone attitude). Sensor matrix now has 2 sensors; pattern scales for adding more (HMC5883L magnetometer, VL53L0X ToF, SSD1306 OLED) trivially. | ✅ done | (prior commit) | [phase_2bd_mpu6050.md](phase_2bd_mpu6050.md) |
| 2.BE | **HMC5883L magnetometer + VL53L0X ToF sensor — 4-sensor matrix**. Extends I2C dispatcher with 2 more cases: 0x1E → HMC5883L (chip-id "H43" at 0x0A-0x0C, slow-rotating 20s compass waveform; honors HMC's quirky X-Z-Y data order), 0x29 → VL53L0X (WHO_AM_I=0xEE at 0xC0, triangular 100-2000mm distance wave at register 0x14). Mechanical extension — ~80 lines of code, no new self-tests, regression-clean. 4 canonical Arduino sensor categories covered: pressure/temp + motion + magnetic + distance. Unlocks compass / obstacle-avoidance / approach-detection / digital-level Arduino demos. | ✅ done | (prior commit) | [phase_2be_hmc_vl53.md](phase_2be_hmc_vl53.md) |
| 2.BF | **🎉 TWAI IRQ wiring — interrupt-driven CAN reception**. Wires `intr_out` GPIO line from TWAI device to CLIC cause 21. INT_RAW.RX bit set when frame loaded; INT_ENA gates propagation; reads of INTR register clear-on-read per SJA1000. New `twai_irq` JSON event tracks level transitions (mirrors `timg_irq`). Self-test extended to enable INT_ENA → load frame (raise) → INTR read (clear) → consume → next frame (re-raise). 5-event boot sequence proves bidirectional + IRQ chain. Pattern template established for I2C/SPI/UART IRQ adoption. **20 JSON event types**. | ✅ done | (prior commit) | [phase_2bf_twai_irq.md](phase_2bf_twai_irq.md) |
| 2.BG | **🎉 I2C IRQ wiring (both buses)** — first backport of the 2.BF template to another peripheral. I2C0 → CLIC cause 22, I2C1 → cause 23. CMD.STOP write sets INT_RAW.{END_DETECT,TRANS_COMPLETE} (0x88). INT_CLR is W1TC (different from TWAI's clear-on-read). INT_ST = INT_RAW & INT_ENA hardware-maintained. New `i2c_irq` JSON event. Self-test refactored to do real CMD-register STOP writes (vs prior helper-only emit). 4 events at boot (2 per bus × raise+clear) prove per-instance IRQ independence. Template now proven for 3 peripherals (TIMG, TWAI, I2C). **21 JSON event types**. | ✅ done | (prior commit) | [phase_2bg_i2c_irq.md](phase_2bg_i2c_irq.md) |
| 2.BH | **SPI2 IRQ wiring** — third backport of the template (after 2.BF/2.BG). USR transaction completion sets `INT_RAW.TRANS_DONE` (bit 0); INT_CLR is W1TC (same as I2C). SPI2 → CLIC cause 24. New `spi_irq` JSON event. 2 events at boot (raise+clear). Template recipe productivity: ~50 lines of code + ~20 minutes (vs 2.BF's ~200 lines + multi-hour design). Unlocks Arduino interrupt-driven SPI / TFT_eSPI DMA-completion handlers. **22 JSON event types**. | ✅ done | (prior commit) | [phase_2bh_spi_irq.md](phase_2bh_spi_irq.md) |
| 2.BI | **RMT IRQ wiring** — fourth backport of the template. `flush_channel()` (pixel decode completion) sets `INT_RAW.CH<N>_TX_END` (per-channel bit). RMT → CLIC cause 25. New `rmt_irq` JSON event. 2 events at boot (raise+clear). Unlocks Arduino Adafruit_NeoPixel `strip.show()` interrupt-driven completion callbacks. 5 peripherals now share the unified IRQ pattern (TIMG/TWAI/I2C/SPI/RMT). **23 JSON event types**. | ✅ done | (prior commit) | [phase_2bi_rmt_irq.md](phase_2bi_rmt_irq.md) |
| 2.BJ | **ADC IRQ wiring** — fifth backport of the template. Guest sample read sets `INT_RAW.ADC1_DONE` (bit 0); edge detection prevents per-read IRQ flood. ADC → CLIC cause 26. Required adding `storage[]` to the previously-pure-compute ADC struct. Inline machine self-test (vs helper function) writes INT_ENA → reads DATA → writes INT_CLR. New `adc_irq` JSON event. 2 events at boot. Unlocks Arduino interrupt-driven `analogRead()` / continuous-mode ADC sketches. **24 JSON event types**. | ✅ done | (prior commit) | [phase_2bj_adc_irq.md](phase_2bj_adc_irq.md) |
| 2.BK | **LEDC IRQ wiring** — sixth backport of the template. Per-channel DUTY register write sets `INT_RAW.CH<N>_DUTY_END` (bit 8+N for 8 channels; bits 0..3 reserved for future TIMER_OVF). LEDC → CLIC cause 27. New `ledc_irq` JSON event. 2 events at boot (raise+clear with `int_raw=0x100` confirming bit 8). Unlocks Arduino `ledcAttachInterrupt()` fade-done callbacks. 7 peripherals now share the unified IRQ pattern (TIMG/TWAI/I2C/SPI/RMT/ADC/LEDC). **25 JSON event types**. | ✅ done | (prior commit) | [phase_2bk_ledc_irq.md](phase_2bk_ledc_irq.md) |
| 2.BL | **TWAI1 + TWAI2 instantiation — 3/3 CAN inventory complete**. Adds TWAI1 (0x500A7000, cause 28) and TWAI2 (0x500A8000, cause 29) as instances of the existing TYPE_ESP32P4_TWAI. New `port_num` field on TWAI state (0/1/2) for JSON disambiguation; all 3 emit paths (TX, RX, IRQ) now include `"port":N`. Each bus fires the full 5-event self-test (TX + raise + clear + RX + re-raise) → 15 TWAI events at boot with proper per-port tagging. Multi-instance pattern now applied across 3 device classes (UART/I2C/TWAI). | ✅ done | (prior commit) | [phase_2bl_twai_multi.md](phase_2bl_twai_multi.md) |
| 2.BM | **🎉 TIMG WDT actual reset action — chip reboots on hang**. Major realism upgrade. When `WDTCONFIG0.EN` is set (unlocked), QEMUTimer arms with 5s timeout; `WDTFEED` resets it; CONFIG0 EN=0 disarms. On timeout: emit `wdt_reset` JSON event + optionally call `qemu_system_reset_request()` (gated on `VELXIO_WDT_RESET=1` so test harnesses can observe without rebooting QEMU). Boot regression-clean: IDF unlock/disable/feed/lock sequence keeps WDT disabled → 0 spurious resets. Applies to both TIMG0 and TIMG1 via shared QOM class. **26 JSON event types**. | ✅ done | (prior commit) | [phase_2bm_wdt_reset.md](phase_2bm_wdt_reset.md) |
| 2.BN | **🎉 RTC + Super WDT reset + TRM silicon-correctness fix**. Completes the 4/4 WDT inventory with reset action. **Major TRM-grounded bug fix discovered**: Phase 2.AT used `0x8F1D312A` for SWD write-protect key (carry-over from older ESP32 generation); TRM ESP32-P4 v0.5 § 17.3.2.2 explicitly says `0x50D83AA1` (same as RWDT/MWDT). Also fixed bit-layout bug: bit 31 of SWD_CONFIG is FEED (write-1), not EN — per TRM Register 17.8. RTC WDT: 5s timeout, CONFIG0.EN gated. SWD: 1s timeout (TRM "slightly less than one second"), DISABLE bit gated. Boot regression-clean (0 wdt_reset events). All bit definitions and constants now TRM-cited inline. | ✅ done | (prior commit) | [phase_2bn_lp_wdt_reset.md](phase_2bn_lp_wdt_reset.md) |
| 2.BO | **🎉 TIMG WDT stage-0 action decoding per TRM Register 16.10**. Phase 2.BM hardcoded "always-system-reset"; TRM specifies a 2-bit STG0 action field at CONFIG0[30:29] with 4 codes (0=none, 1=intr, 2=rst_cpu, 3=rst_sys). New `wdt_irq` JSON event for action=1; `wdt_reset` event now carries `action:` field. Reset call only fires for action ∈ {2,3} + env var opt-in. **Additional latent silicon bug found+fixed**: Phase 2.AP defined `WDT_FLASHBOOT_EN` at bit 30 — but TRM puts it at bit 15 (bit 30 is the STG0 action high bit). All WDT bit definitions now TRM-cited inline. Boot regression-clean. **27 JSON event types**. | ✅ done | (this commit) | [phase_2bo_wdt_stg0_action.md](phase_2bo_wdt_stg0_action.md) |
| **next** | TBD: RWDT action decoding, multi-stage WDT, WDT IRQ→CLIC wiring, UART IRQ, real PWM, BH1750/SHT31/CCS811, SPI3, FreeRTOS (deferred) | ⏭️ choice | — | — |
| 2.V | `mnxti`/`mintstatus` real semantics (multi-level preemption) | ⏳ planned | — | TBD |
| 2.B | TIMG real (timers + WDT) | ⏳ pending | — | (see roadmap) |
| 2.C | HP_SYSREG + Reset/Clock real | ⏳ pending | — | (see roadmap) |
| 2.D | CLIC + Interrupt Matrix | ⏳ pending | — | (see roadmap) |
| 2.E | eFuse extended (chip rev, BLK0) | ⏳ pending | — | (see roadmap) |
| 2.F | SYSTIMER complete | ⏳ pending | — | (see roadmap) |
| 2.G | GPIO + IO MUX full | ⏳ pending | — | (see roadmap) |
| 2.H | UART complete (5 ports + IRQ) | ⏳ pending | — | (see roadmap) |
| 2.I | RNG + USB Serial/JTAG + crypto | ⏳ pending | — | (see roadmap) |
| 2.J | Multi-core HP + LP | ⏳ pending | — | (see roadmap) |
| 2.K | I2C, SPI master, LEDC, ADC, RMT | ⏳ pending | — | (see roadmap) |
| 1.F | SPI flash controller real (P4 no tiene uno separado — ver lite) | ⏸️ N/A | — | [phase_1f_spi_flash.md](phase_1f_spi_flash.md) |
| 1.G | Cache MMU (flash window translation) | ⏳ pending | — | [phase_1g_cache_mmu.md](phase_1g_cache_mmu.md) |
| 1.H | TIMG con WDT auto-disable | ⏳ pending | — | [phase_1h_timg_wdt.md](phase_1h_timg_wdt.md) |
| 1.I | HP_SYSREG + Reset/Clock real (smart stub ya cubre lo crítico) | ⏳ pending | — | [phase_1i_hp_sysreg.md](phase_1i_hp_sysreg.md) |
| 1.J | RNG, eFuse extendido, segundo HP core | ⏳ pending | — | [phase_1j_misc.md](phase_1j_misc.md) |
| **1.K** | **Interrupt Matrix + UART real-completo (IRQ wiring)** | ⏭️ **next** | — | [phase_1k_uart_full.md](phase_1k_uart_full.md) |
| 1.L | **iterative-patch loop** — 50+ patches needed para llegar a app_main | ⏳ pending | — | [phase_1l_iterative_patches.md](phase_1l_iterative_patches.md) |
| 2 | Arduino blink visible end-to-end | ⏳ pending | — | [phase_2_blink.md](phase_2_blink.md) |
| 3 | Peripherals avanzados (I2C, SPI master, LEDC, ADC, RMT) | ⏳ pending | — | [phase_3_advanced.md](phase_3_advanced.md) |
| 4 | Integración Velxio backend (`esp_qemu_manager.py`) | ⏳ pending | — | [phase_4_velxio_integration.md](phase_4_velxio_integration.md) |
| 5 | LP core + USB Serial/JTAG | ⏳ later | — | [phase_5_lp_core.md](phase_5_lp_core.md) |

**Out of scope (no planeado):**
- MIPI-CSI / MIPI-DSI / ISP / JPEG / H264 codec
- USB OTG HS bus emulation
- Full crypto accelerators (AES/SHA/RSA/HMAC fuera de stubs RW)

## Cómo usar este folder al volver

1. Abrir este README.
2. Mirar la fila marcada "**next**".
3. Abrir el `.md` correspondiente — tiene goal, acceptance criteria, archivos a tocar, y próximos pasos concretos.
4. Cuando termine la fase, mover el "next" a la próxima fila y actualizar la columna Commit.

## Convenciones

- ✅ done — fase implementada + commit landeado + verificación pasó.
- ⏭️ next — la próxima a tocar, con plan ya escrito.
- ⏳ pending — definida en archivo pero sin trabajo aún.
- 🚧 in-progress — work in progress (commit no pusheado todavía).
- ❌ blocked — depende de algo externo.

Cada archivo de fase tiene:
- **Goal** — qué se completa cuando esta fase está done.
- **Acceptance criteria** — cómo verifico que está done.
- **Archivos a tocar** — paths concretos.
- **Pasos** — checklist accionable.
- **Notes** — cosas que me olvido siempre.

Para evitar drift entre `IMPL_STATUS.md` (cronológico, narrativo) y este folder (estructurado, accionable):
- `IMPL_STATUS.md` cuenta la historia en orden cronológico, lo que ya pasó.
- `stages/` es el plan, ordenado por dependencia, lo que va a pasar.
- Después de completar una fase: actualizar AMBOS (commit en `IMPL_STATUS.md`, status flip en `stages/README.md`).
