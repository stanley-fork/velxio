// Open-source no-op stub for `@pro/seoRoutes` (see vite.config.ts). The
// velxio-prod overlay adds its per-board simulator landings here; an OSS
// build has no marketing surface, so it contributes no routes.
import type { SeoRoute } from '../seoRoutes';

export const PRO_SEO_ROUTES: SeoRoute[] = [];
